import React from 'react';
import PropTypes from 'prop-types';

const Simple = ({text}) => {
    return (
        <div>
            
        </div>
    );
};

Simple.defaultProps={
}

Simple.propTypes = {
    text: PropTypes.string
};

export default Simple;